let arr = [5, 3, 8, 1];

let filtered = filterRange(arr, 1, 4);

alert( filtered ); // 3,1 (matching values)

alert( arr ); // 5,3,8,1 (not modified)

//Note: I was solving and felt something was wrong, turns out I didn't understand the problem and was solving something else, so I discarded it.
// Coped the solution from javascript.info 